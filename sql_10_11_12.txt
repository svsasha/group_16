10

SELECT model, price
from printer
WHERE price = (SELECT MAX(price) 
 FROM printer
 )

11

SELECT avg(speed) as avg_speed
FROM PC

12

SELECT AVG(speed) as avg_speed
FROM laptop
WHERE price > 1000
